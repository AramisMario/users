server:
  port: 8080
spring:
  application:
    name: "users"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  datasource:
    url: "jdbc:mysql://localhost:3306/talentpooljava"
    username: "root"
    password: "Ka55#27uTJ9s"
    driverClassName: "com.mysql.cj.jdbc.Driver"
  jpa:
    #databasePlatform: "org.hibernate.dialect.H2Dialect"
    databasePlatform: org.hibernate.dialect.MySQLDialect
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
